# developp a bmi calculator  using the following dimensions kg and m

weight = float(input("Enter the  weight"))
height = float(input("Enter the  height"))
bmi = (weight)/(height ** height)
print(round(bmi))