# Operator       Example            Meaning
# And            a and b            If x is false returns x else returns y
# Or             a or  b            if x is true returns x else returns  y
# Not            not a              if a is false returns true, else False
